CLAIM 8 - Paper claim 5: threat hunting on held-out samples
================================================================================

CLAIM (paper claim 5, figures in Claim5_Threathunting/)
    In the threat-hunting experiment each cluster is split into training samples,
    from which the rule is generated, and held-out test samples (the Ratio_0.75
    split). On the held-out samples AutoPYara's rules keep a higher TP rate than
    AutoYara's (mean of the TP curve, sdhash clusterings, thresholds 50-90 pooled):

                                        train            test (held out)
                                  AutoPYara AutoYara   AutoPYara AutoYara
        with heuristics only        81.9 %   69.8 %      80.8 %   69.4 %
        without heuristics          80.4 %   75.0 %      76.3 %   75.8 %

    (Claim wording derived from the figure names and the plotted values; the
    numbers are what the provided data produce.)

FIGURES
    Claim5_Threathunting/sdhash_WithHeuOnly_IdealPlot.pdf     experiment Exp1HEUONLYsdhash
    Claim5_Threathunting/sdhash_WithNOHeuOnly_IdealPlot.pdf   experiment Exp1NOHEUsdhash

    Mirrored plot: training samples on the left, held-out test samples on the
    right; x = cluster size (samples evaluated), y = running mean of the per-size
    median TP rate. Purple = AutoPYara, brown = AutoYara; dotted lines = curve means
    (the numbers above).

INPUTS
    data/ThreatHunting/{Exp1HEUONLYsdhash,Exp1NOHEUsdhash}/Th{50..90}/Ratio_0.75/
        cluster_*/tprate{AutoPYara,AutoyaraBase}_{Train,Test}.txt (+ train/test lists)

HOW TO RUN
    ./run.sh [-j N]

    Prerequisites: ../../install.sh and the evaluation data in ../../data.
    Runtime: a few seconds.

WHAT IS VERIFIED (artifact/plots/verify_claims.py)
    1. Both figures are regenerated from the data.
    2. Every plotted number matches expected/values.json within 1.0 percentage point.
    3. The claim holds: in both experiments AutoPYara's held-out (test) curve mean
       is higher than AutoYara's.

EXPECTED RESULT
    "CLAIM 8: PASS". Reference figures: expected/figures/. Transcript:
    expected/expected_output.txt.

NOTE ON MARGINS
    With heuristics only, AutoPYara leads by about 11 points on held-out samples.
    Without heuristics the lead is small (about 0.5 points), so that half of the
    claim rests on the direction, not on a large margin.

TOLERANCE / SCOPE
    As for claim 4.
